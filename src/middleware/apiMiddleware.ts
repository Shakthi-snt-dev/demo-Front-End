// import { Middleware } from '@reduxjs/toolkit'
// import type { RootState } from '@/store'

// /**
//  * API Middleware that intercepts all API requests
//  * and automatically adds the authentication token if available
//  */
// export const apiMiddleware: Middleware<{}, RootState> = (store) => (next) => (action) => {
//   // This middleware can be extended to intercept API calls
//   // For now, the token is handled in the API client
//   // But we can add request interceptors here if needed
  
//   return next(action)
// }

